<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubFirstSubmission" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/blue_primary_dark</item>
        <item name="colorSecondary">@color/pink_secondary</item>
        <item name="android:textColorPrimary">@color/white_text</item>
        <item name="iconTint">@color/white</item>
        <item name="drawableTint">@color/white</item>
    </style>
</resources>